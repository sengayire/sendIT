
import uuid from 'uuid/v1';

const parcels = [
  {
    id: uuid(),
    from: 'huye',
    sender: 'prince',
    destination: 'kigali',
    riciever: 'mutabazi',
    status: 'delivered',
  },
];

export default parcels;
